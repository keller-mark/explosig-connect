To run the notebooks containing case studies, create and activate the case study conda environment:

```sh
conda env create -f environment.yml
source activate explosig-connect-case-study-env
```

Start jupyter:

```sh
jupyter notebook
```